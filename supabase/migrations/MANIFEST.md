# Migration Manifest — DeskcommCRM

Migrations applied to Supabase project `rrydmwnporysaiysiztn` (sa-east-1, Postgres 17) via Supabase MCP on 2026-04-28.

## Applied

| Version | Name | Description |
|---|---|---|
| `20260428195354` | `0001_platform_base` | organizations, user_organizations, platform_admins, api_tokens, api_audit_log, user_recovery_codes, idempotency_keys + RLS helpers (fn_user_org_ids, fn_is_platform_admin, fn_user_role_in_org, fn_role_at_least) |
| `20260428195513` | `0002_event_log_and_compat` | event_log + emit_event/fn_log_event helpers + compat aliases (fn_set_updated_at, fn_user_role_in returning int) |
| `20260428195708` | `0003_customer_360` | contacts (CPF encrypted), crm_pipelines, crm_stages, crm_leads, crm_lead_activities, crm_lead_links, merge_queue + 5 domain triggers |
| `20260428200016` | `0004_whatsapp_waha` | channel_sessions, channel_session_warmup, conversations, messages, webhook_events_log + emit_message_event trigger |
| `20260428200128` | `0005_ai_rag` | ai_agents, ai_knowledge_sources, ai_chunks (vector(1536) ivfflat), ai_knowledge_versions, ai_invocations, ai_pricing (3 seeded), ai_budgets + fn_audit_log_row helper |
| `20260428200211` | `0006_nuvemshop_lgpd` | tenant_integrations, orders, nuvemshop_products, lgpd_requests + fn_encrypt_oauth/fn_decrypt_oauth + LGPD/DLQ extra indexes on webhook_events_log |
| `20260428200331` | `0007_security_hardening` | search_path=public set on all functions, ai_pricing public-read policy, revoke EXECUTE anon on internal helpers, tighten api_audit_log INSERT policy |
| `20260429013958` | `0008_tenant_onboarding_state` | onboarding state machine columns + transitions on organizations |
| `20260429021857` | `0009_expand_messaging_constraints` | extra check constraints + indexes on conversations/messages for inbox perf |
| `20260429032132` | `0010_ai_rag_handoff_columns_and_rpcs` | EPIC-06 wave 1: contacts.force_human + conversations.bot_silenced_until/last_handoff_at + RPC retrieve_top_k_chunks (security definer + programmatic org filter) + RPC activate_kb_version + ai_pricing seed corrections (haiku 100/500, embedding-3-small 20) |
| `20260429040000` | `0011_handoff_reason_column` | EPIC-06 wave 3: conversations.last_handoff_reason (diagnostic) + crm_stages.requires_human (gate G4 — bypass bot when lead enters critical stage) |
| `20260429060000` | `0012_kb_version_lifecycle_columns` | EPIC-06 wave 4: ai_knowledge_versions lifecycle columns — status (building/ready/failed), error_message, indexed_at |
| *(wave 5)* | `0013_ai_faq_items` | EPIC-06 wave 5: ai_faq_items table (RLS via fn_user_org_ids) + name/status/ingested_at columns on ai_knowledge_sources + expanded source_type check |
| `20260429080000` | `0014_storage_policies_ai_policy` | EPIC-06 wave 6: private ai-policy bucket (20MB cap, pdf+md MIME) + per-tenant SELECT/INSERT/DELETE RLS on storage.objects (path-prefix org isolation via user_organizations EXISTS subquery) |
| `20260429090000` | `0015_conversations_rag_optin` | EPIC-06 wave 7 (S-06.07, LGPD L-08): conversations.usable_for_rag + marked_at + marked_by + rag_review_status (tri-state) + partial index on (org, usable_for_rag, marked_at) where true |
| `20260428000000` | `0016_lgpd_emergency_scope` | EPIC-08 wave 3: lgpd_requests.emergency (boolean, default false) + scope (text check 'contact'/'tenant', default 'contact') + partial index lgpd_requests_emergency_idx on (org, emergency, due_at) where emergency=true |
| `20260429100000` | `0017_storage_policies_lgpd_exports` | EPIC-08 wave 4 (S-08.04): private lgpd-exports bucket (50MB cap, pdf+json MIME) + per-tenant SELECT RLS on storage.objects (path-prefix org isolation via user_organizations EXISTS subquery). Worker uploads via service-role only — no INSERT/DELETE policies for anon/authenticated. |
| `20260429110000` | `0018_lgpd_redaction_queue` | EPIC-08 wave 5 (S-08.05): storage_redaction_queue table (org-scoped RLS, unique (bucket, object_path), partial index on pending) — async drain target for LGPD media deletion. |
| `20260429110001` | `0019_lgpd_cascade_redact_rpc` | EPIC-08 wave 5 (S-08.05, L-04): SECURITY DEFINER fn_lgpd_cascade_redact_contact(org, contact, request) — atomic 8-step cascade (contacts irreversible + conversations + messages + activities + leads + orders payload strip + media enqueue + audit). ACL revoked from anon/authenticated; granted only to service_role. |
| `20260429120000` | `0020_organization_suspend_reason` | EPIC-11 wave 8 (S-11.08): organizations.suspended_reason (text) + suspended_by (uuid → auth.users) — enables suspend/reactivate API to store reason + actor. |
| *(wave 11)* | `0021_incidents` | EPIC-11 wave 11 (S-11.11): incidents table (organization_id optional FK, severity check info/warning/critical, status open/acknowledged/resolved, payload jsonb, acknowledged/resolved actor+timestamp, resolution_note, RLS via fn_is_platform_admin — platform-admins only, users do not see). Indexes on status+created_at (partial, excludes resolved), org+created_at, severity+status. |
| `20260429140000` | `0022_ai_budget_trigger` | EPIC-06 finalize: ai_budget enforcement trigger on ai_invocations. |
| `20260505140000` | `0023_ai_agents_module` | EPIC-13 wave 1 (S-13.01): foundation schema for configurable AI Agents — extends ai_agents (published_version_id, priority, archived_at, kind), creates ai_agent_versions, ai_provider_credentials (+ safe view, security_invoker), ai_agent_runs (partial unique one_running_per_conv), ai_models (global catalog, 8 models seeded). RLS via fn_user_org_ids on tenant-aware tables; audit triggers via fn_audit_log_row (helper inlined idempotently — was missing from local migrations although present on remote since 0005). ai_models is global read-all, write via service role only. |
| `20260506000000` | `0024_ai_agent_publish_fn` | EPIC-13 S-13.06: fn_publish_ai_agent_version (atomic Save/Publish flip) — validates agent/version/credential/channel_session/model before flipping published_version_id. |
| `20260506100000` | `0025_fix_publish_fn_and_realtime_publication` | Forward-fix: qualifies column refs in fn_publish_ai_agent_version to resolve `agent_id` ambiguity against RETURNS TABLE output params; adds ai_agent_runs/ai_agents/ai_knowledge_sources to the supabase_realtime publication. |
| `20260706200000` | `0026_fix_publish_fn_status_case` | Forward-fix: fn_publish_ai_agent_version compared channel_sessions.status against lowercase `'working'`, but the canonical value (channel_sessions_status_check, written by the WAHA webhook handler) is uppercase `'WORKING'`. Publish always raised `channel_session_offline`, even for a genuinely connected session. Bug present since 0024, carried forward unchanged by 0025. |
| `20260717140000` | `0052_project_notes_position` | Quadro de briefing LIVRE: `project_notes += pos_x, pos_y` (posição no plano/malha 3D estilo AutoCAD). Substitui o modelo de raias da 0051 na UI (phase/sort_order/project_phases ficam forward-only, sem uso). Apêndice idempotente no baseline. |
| `20260717130000` | `0051_project_board` | Quadro branco de briefing: `project_notes` ganha `phase` (raia) + `sort_order` (ordem na raia); nova tabela `project_phases` (raias nomeadas, permite raia vazia) org-scoped + RLS + audit. UI vira board de raias com arrastar-entre-raias + zoom. Apêndice idempotente no baseline. |
| `20260717120000` | `0050_consumables` | Consumíveis (filamentos/resinas) — módulo próprio, separado de `inventory_assets` (ativo fixo). Tabela `consumables` org-scoped + RLS + audit: `name`, `category` check (`filamento`/`resina`/`outro`), `material`, `color`, `stock_grams`/`min_stock_grams` (estoque em gramas), `cost_per_kg_cents`, `supplier`, `notes`. Alimenta o botão "Sincronizar" da planilha de Controle (linhas categoria "Filamentos" → consumíveis). Apêndice idempotente no baseline. |
| `20260716260000` | `0049_model_folders` | Explorador de arquivos por cliente na Modelagem. Tabela `model_folders` (árvore livre: `parent_id` self-FK cascade, `icon`, `color`, `contact_id` opcional → CRM, `sort_order` numeric) org-scoped + RLS + audit. Generaliza `models_3d` para guardar qualquer arquivo do cliente: `folder_id` (FK on delete set null — arquivo sobe à raiz, não some), `mime_type`, `kind` check (`stl`/`model3mf`/`image`/`other`), `sort_order`. Bucket `models-3d` já aceita qualquer mime. |
| `20260716250000` | `0048_marketplace_orders` | Vendas de marketplace com lançamento manual (aba Vendas real, saindo dos placeholders "Prévia"). Tabela `marketplace_orders` org-scoped + RLS + audit — `platform` (mesmo check de `financial_records.platform`), status, `total_cents`/`commission_cents`, `sold_at`, `service_order_id` FK opcional. Índice único parcial `(org, platform, external_order_id) where external_order_id is not null` para idempotência da futura integração automática (webhook), sem colidir no manual (external nulo). |
| `20260716240000` | `0047_my_sessions_rpc` | RPC `fn_my_sessions()` SECURITY DEFINER — lista as sessões ativas do próprio usuário (filtro `auth.uid()`) lendo `auth.sessions`, que o PostgREST não expõe. Read-only (id, created/updated/not_after, user_agent, ip); revogação individual mexeria em internals do GoTrue, então fica com o "sair de todos" que já existe. Grant só para `authenticated`. |
| `20260716230000` | `0046_avatars_bucket` | Bucket público `avatars` (5 MB, imagem) para foto de perfil — antes o avatar só aceitava URL colada à mão. Escopo POR USUÁRIO (não por org): caminho `<user_id>/...`, 4 policies em `storage.objects` conferindo `auth.uid()` contra a 1ª pasta (leitura pública; escrita/update/delete só na própria pasta). |
| `20260716220000` | `0045_models_3d` | Repositório de modelos 3D (STL) saindo de client-only efêmero para o banco. Tabela `models_3d` org-scoped + RLS + audit (metadados: nome, `file_path`, triângulos, `volume_cm3`, `bounding_box` jsonb, `thumbnail_url`). Bucket privado `models-3d` (100 MB) + 3 storage policies com isolamento por prefixo `<org_id>/` via `user_organizations` (padrão ai-policy 0014 / landing-media 0042). Arquivo STL sobe direto ao Storage por URL assinada; inspetor 3D baixa e reparseia sob demanda. |
| `20260716210000` | `0044_calendar_events` | Eventos personalizados do Calendário saindo do `localStorage` para o banco (multi-dispositivo/multi-usuário). Tabela `calendar_events` org-scoped + RLS + audit + índice `(organization_id, event_date)`. `type` check `maintenance/meeting/delivery/custom` — eventos de OS (`type 'os'`) continuam derivados de `service_orders`, não duplicados aqui. |
| `20260716180000` | `0043_service_orders_concluded_at` | `service_orders.concluded_at` (timestamptz) + trigger `trg_service_orders_concluded_at` que carimba na transição para `concluido` e **zera ao reabrir**. Corrige o gráfico "Fluxo de O.S." do Dashboard, que usava `updated_at` como data de conclusão — editar uma O.S. concluída a fazia reaparecer como concluída no dia da edição (número errado com cara de certo). Backfill de `updated_at` só onde `concluded_at is null`. Índice parcial `(organization_id, concluded_at) where concluded_at is not null`. |
| `20260716120000` | `0042_landing_media_bucket` | Biblioteca de mídia do Landing Edit: bucket `landing-media` (público na leitura — são as fotos da vitrine, servidas pela CDN; limite 50 MB; mime allowlist de imagem/vídeo) + 4 policies em `storage.objects`. Escrita/exclusão só para membro da org e só dentro da pasta `<organization_id>/`, conferida via `storage.foldername(name)[1]` contra `fn_user_org_ids()` — uma org não apaga a mídia da outra. |
| `20260715210000` | `0041_landing_cms` | Landing Edit: `products` vira a fonte de verdade única da landing pública, que lia um arquivo TS estático (`lib/marketing/products.ts`). Campos de vitrine (`slug` único por org, `is_published`, `is_top`, `bestseller_rank` 1..3 com índice único parcial — um só campeão por org, `sort_order numeric` p/ fractional indexing, `hero_copy`, `price_range`, `links`/`videos`/`colors` jsonb, `material`, `dimensions`) e de comércio (`stock_qty`, `sold_qty`, lançamento manual) somados à engenharia de custo da 0030. Novas tabelas `landing_settings` (textos/banners por seção, 1 linha por org) e `platform_commissions` (% por plataforma, entrada manual; lista espelha o check de `financial_records.platform` da 0037, semeada em 0% p/ toda org). Ambas org-scoped + RLS + audit. **Sem grant a `anon`**: a landing é Server Component e lê via admin client com `organization_id` de env — expor `products` ao anon vazaria `filament_grams`/`extra_costs`/`margin_pct` (a anon key é pública no browser). Dedup defensivo de `slug`/`bestseller_rank` antes dos índices únicos p/ o `update.sh` de clones não quebrar. |
| `20260715030000` | `0040_financial_records_drop_classification` | Funde Venda/Insumo dentro de Categoria: copia `classification` → `category` e derruba `classification` (com seu check Venda/Insumo/Outro). Motivo: `category` valia `(R) Renda Extra` em 100% das linhas (não classificava nada) enquanto o sinal útil vivia preso a 3 valores fixos em `classification`. Agora `category` é texto livre com rótulos geridos pelo usuário na planilha. **Publique o código que parou de escrever em `classification` ANTES de aplicar.** Idempotente. |
| `20260715020000` | `0039_printers_hours_used` | `printers.hours_used` (numeric, default 0, check >= 0) — horímetro da máquina. Não é derivável de `print_jobs.print_time_seconds`, que só soma o que o CRM registrou: uma impressora chega usada (a Bambulab A1 entrou com 1217H). Distinto de `depreciation_per_hour` (custo/hora) e do `vidaUtil` da calculadora (vida útil esperada). Idempotente. |
| `20260714204500` | `0038_financial_records_custom_fields` | `financial_records.custom_fields` (jsonb, default `{}`) — colunas customizadas criadas pelo usuário na planilha de Controle. Idempotente. |
| `20260714203900` | `0037_financial_records_platform` | `financial_records.platform` (text, check B2B/Shopee/Facebook/Mercado Livre/TikTok Shop/Olx/''/Outro ou null) — canal de venda do lançamento. Idempotente. |
| `20260714202000` | `0036_financial_records` | Planilha de controle financeiro: tabela `financial_records` (date, month, quantity, description, type Receita/Despesa, category, classification Venda/Insumo/Outro, revenue_cents, expense_cents, installments). Org-scoped + RLS + audit + índice `(organization_id, date desc)`. Idempotente. |
| `20260711160000` | `0035_suppliers` | Cadastro real de fornecedores + histórico de compras (fora do localStorage): tabelas `suppliers` (name, category filament/printer/shipping/tools/other, contact_person, phone, website, rating 1-5, avg_delivery_days, notes) e `supplier_purchases` (supplier_id FK on delete set null + supplier_name snapshot, item_name, qty, unit_price_cents, purchased_at). Ambas org-scoped + RLS + audit. Idempotente. |
| `20260711150000` | `0034_projects` | Projetos técnicos + quadro de ideias saindo do localStorage para o banco: tabelas `projects` (parâmetros de fatiamento — filamento, peso, horas, camada, infill, velocidade, temps, custo/kg, watts, kwh, depreciação) e `project_notes` (post-its: title/content/color yellow·pink·blue·green). Ambas org-scoped + RLS + audit. Idempotente. |
| `20260711140000` | `0033_inventory_assets` | Inventário de ativos fixos da oficina: tabela `inventory_assets` (org-scoped + RLS `tenant_isolation_*_all` + audit, índices por org/status). Campos: `name`, `category` (impressora/ferramenta/movel/computador/estufa/eletronico/outro), `quantity`, `purchase_value_cents`, `purchase_date`, `useful_life_months` (depreciação linear), `status` (ativo/manutencao/inativo), `notes`. Idempotente. |
| `20260711130000` | `0032_print_jobs_service_order` | Vincula impressão à OS: adiciona `print_jobs.service_order_id` (uuid, FK `service_orders(id)` on delete set null, nullable) + índice `(organization_id, service_order_id)`. Permite atribuir a impressão que originou cada job e abrir a OS a partir da fila. Idempotente. |
| `20260711120000` | `0031_service_orders_stages` | Expande o funil de `service_orders`: novo `status` check com 6 estágios (`orcamento`, `aprovado`, `em_producao`, `pos_processamento`, `pronto_entrega`, `concluido` — `concluido` preservado pois sustenta o faturamento do dashboard). Adiciona `priority` (text, check `alta`/`media`/`baixa`, default `media`) e `material` (text nullable, ex. PLA/ABS/PETG) para os cards estilo Deals. Idempotente. |
| `20260710180000` | `0030_products_catalog` | Catálogo de produtos com engenharia de custo (BOM): tabela `products` (org-scoped + RLS + audit). Custo real = gramas filamento × custo/g (via `filament_client_id`) + energia + depreciação (`printer_client_id`) + `extra_costs jsonb` (insumos), com `margin_pct` → preço sugerido. `images jsonb` (galeria), `sale_price_cents` (override manual opcional). |
| `20260710160000` | `0029_service_orders` | Ordens de Serviço: tabela `service_orders` (org-scoped + RLS `tenant_isolation_*_all`, índices por status/SLA, audit trigger). Board orçamento→aprovado→em_producao→concluído com `position` para ordenação drag-and-drop, `contact_id` (FK contacts, on delete set null) + `contact_name` snapshot, `total_cents`, `qty`, `sla_due_at`, `slicer_notes jsonb`. |
| `20260710120000` | `0028_printers_module` | Fazenda de impressão 3D: tabelas dedicadas `filaments`, `printers`, `print_jobs` (org-scoped + RLS `tenant_isolation_*_all` via `fn_user_org_ids`, índices, audit trigger), substituindo o armazenamento anterior em `organizations.settings` (jsonb) que sofria read-modify-write concorrente e não tinha RLS por linha. PK uuid + `client_id` text único por org (mantém os ids gerados no frontend, ex. `prn_1`, sem reescrever a UI). Habilita baixa de estoque row-level pelo webhook de telemetria (OctoPrint/Klipper). `k_energy` (tarifa) permanece em `organizations.settings`. |
| `20260706210000` | `0027_whatsapp_conversation_unification` | Bugfix de governança de conversas WhatsApp. Causa-raiz: contatos @lid sem unique key + resolução check-then-act, e o WAHA emitindo `message`+`message.any` por mensagem → 1 pessoa virava N contatos/conversas (medido: 1 lid = 12 contatos). Adiciona coluna gerada `contacts.wa_identity` (`phone:+E164`/`lid:<digits>`), faz merge idempotente do histórico duplicado repontando todas as FKs (usa `is_merged_into` como mapa, sem temp tables → portável em psql), cria `uniq_contacts_org_wa_identity` + `uniq_conversations_1to1_per_contact_session` (a antiga unique incluía group_chat_id NULL, que no Postgres não protege 1:1), e as funções de upsert atômico `fn_upsert_wa_contact`/`fn_upsert_wa_conversation`/`fn_mark_conversation_message` que a app passa a usar (lib/waha/ingest.ts) no lugar do check-then-act. Também corrige mapeamento de `type` WAHA→CRM (`chat`→`text` etc.) que fazia mensagens reais violarem messages_type_check e sumirem. |

## Reproducibility

Migrations were applied directly via the Supabase MCP `apply_migration` tool during the autonomous bootstrap session. The SQL of each migration is also embedded in the corresponding spec under `docs/specs/0X-spec-*.md` and the database keeps them in `supabase_migrations.schema_migrations`.

To re-apply on a fresh Supabase project, replay the migrations in version order via `supabase db push` (Supabase CLI) or via the MCP.

## Tables created (33 total, all RLS enabled)

- **Platform**: organizations, user_organizations, platform_admins, api_tokens, api_audit_log, user_recovery_codes, idempotency_keys
- **Bus**: event_log
- **Customer 360**: contacts, crm_pipelines, crm_stages, crm_leads, crm_lead_activities, crm_lead_links, merge_queue
- **WhatsApp**: channel_sessions, channel_session_warmup, conversations, messages, webhook_events_log
- **AI**: ai_agents, ai_knowledge_sources, ai_knowledge_versions, ai_chunks, ai_invocations, ai_pricing (global), ai_budgets, ai_faq_items
- **Integrations**: tenant_integrations, orders, nuvemshop_products
- **Compliance**: lgpd_requests
- **Ops**: incidents
